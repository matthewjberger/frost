// This should fail - use after move
Point :: struct { x: i64, y: i64 }

consume :: proc(p: Point) {
    print(p.x);
}

p := Point { x = 10, y = 20 };
consume(p);
print(p.x);  // Error: use of moved value
